<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Emoji or Icon on the left (optional) -->
        <ImageView
            android:id="@+id/habit_icon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="start"
            android:src="@drawable/habit" />

        <!-- Habit name -->
        <TextView
            android:id="@+id/habit_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Habit name"
            android:textColor="@color/lite_purple"
            android:textStyle="bold"
            android:textSize="16sp" />

        <!-- Habit detail (after habit) -->
        <TextView
            android:id="@+id/habit_detail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="After: habit detail"
            android:textColor="@color/light_blue"
            android:textSize="14sp" />
    </LinearLayout>

</androidx.cardview.widget.CardView>
